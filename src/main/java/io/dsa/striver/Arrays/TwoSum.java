package io.dsa.striver.Arrays;

public class TwoSum {
    static void TwoPointer(int[] arr, int k) {
        int left = 0;
        int right = 0;
        while (left < right) {
            // if we want more than the target then increase the index (left) otherwise just decrease (right)
        }
    }
}
